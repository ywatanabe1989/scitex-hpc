"""scitex-hpc MCP (Model Context Protocol) server package.

The actual ``FastMCP`` instance lives in :mod:`scitex_hpc._mcp.server`.
Importing it here is deferred so that ``scitex-hpc`` remains usable
without the optional ``fastmcp`` dependency.
"""
